# Direct runtime dependencies. Compiled to requirements.txt, which is what the
# image installs - exact versions plus hashes. Regenerate with `make lock` after
# editing this file; never hand-edit requirements.txt.
#
# Bounds here express what we support, not what we install: the lock decides that.

# Django
Django>=5.2,<6.0

# Database
dj-database-url>=2.1.0
psycopg[binary]>=3.1.0

# Environment
python-dotenv>=1.0.0

# HTTP client for CFSSL API and the Mailgun backend
requests>=2.31.0

# Encryption for private keys and stored credentials
cryptography>=41.0.0

# REST API
djangorestframework>=3.14.0
drf-spectacular>=0.27.0  # OpenAPI/Swagger documentation

# Production server
gunicorn>=21.0.0

# Static files serving
whitenoise>=6.6.0

# QR code generation for the trust portal
qrcode>=7.4.0

# Optional email backends. Both are imported lazily by core/services/email.py, but
# they ship in the image so that choosing SendGrid or SES in the settings UI works
# without a rebuild.
sendgrid>=6.10.0
boto3>=1.34.0
